# Docker runtime dependencies for the Hy-MT2 service path.
# Legacy CTranslate2 translation is intentionally not installed in the image;
# use requirements.txt for a local/non-Docker installation that needs it.
numpy==2.4.6
soundfile==0.14.0
sounddevice==0.5.6
sherpa-onnx==1.13.6
onnxruntime==1.29.0
fugashi==1.5.2
unidic-lite==1.0.8
psutil==7.2.2
huggingface_hub==1.28.0
